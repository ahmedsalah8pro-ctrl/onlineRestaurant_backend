# onlineRestaurant_backend

API-only Laravel 13 restaurant backend.

Current synced status:
- Remote sync performed through GitHub API because local `git push` still requires interactive GitHub auth on this machine.
- Latest verified local commit: `da101e3`
- Test server: `http://165.245.163.125`
- Health endpoint: `GET /api/v1/health`

This repository currently contains the latest backend operational improvements and backend docs that were synced from the local workspace.
